{
  "scripts": {
    "deploy": "elm-app build && gh-pages -d build"
  },
  "devDependencies": {
    "postcss-loader": "^3.0.0",
    "tailwindcss": "^1.2.0",
    "webpack-merge": "^4.2.2"
  },
  "dependencies": {
    "copy-to-clipboard": "^3.3.1",
    "tippy.js": "^6.2.6"
  }
}
